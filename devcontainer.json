{
  "name": "Haskell & Python Dev Container",
  "dockerFile": "Dockerfile",
  "workspaceFolder": "/workspace",
  "extensions": [
    "haskell.haskell",
    "ms-python.python",
    "ms-vscode.cpptools"
  ],
  "settings": {
    "python.pythonPath": "/venv/bin/python"
  },
  "postCreateCommand": "pip install --upgrade pip && pip install -r requirements.txt"
}
